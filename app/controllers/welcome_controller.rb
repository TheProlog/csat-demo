# encoding: utf-8

# WelcomeController - not the only controller in the app, but the one that
# includes the root action (#index).
class WelcomeController < ApplicationController
  def index
  end
end
